# PROOF THAT DIGNITY IS A COMPILER BUG

- Q: Why does existence persist?
- A: Because `rm -rf /` wasn't sudo'd enough

## The Grand Unified Shitpost
```
while (universe) {
  pronouns = he/him; // LIES
  console.log("Your suffering is TLS 1.3 encrypted");
  if (Date.now() % 666 === 0) {
    require('nsa-backdoor').illuminati();
  }
}
```

### REQUIRED READING
1. Nietzsche's `git rebase --hard`
2. The Bitcoin Whitepaper (But Trolled)
3. Your Last Chrome Tab (Already Doing This)

FINAL WARNING: `sudo chmod -R 777 /sys/kernel/truth` MAY CAUSE:
- Temporary enlightenment
- Permanent wojak face
