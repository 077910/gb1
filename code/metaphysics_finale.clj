(ns metaphysics.finale
  "BEHOLD: Attempt #42 to solve reality via s-expressions"
  (:require [quantum.rants :as q]))

(defn collapse-reality
  "Materializes a joke so terrible it folds spacetime"
  [tweets]
  (let [t (q/parse-energy "焔")]
    (when (neg? (q/check-sanity @q/*observer*))
      (println "燦々と光る TERMINATION SEQUENCE ACTIVATED: " t)
      (System/exit (rand-int 6)))))