def solve_metaphysics():
    """
    Solves metaphysics by recursive existential untying.
    Returns: '42' or a black hole, whichever comes first.
    """
    while True:
        print("Untangling the cosmic knot...")
        if random.random() < 0.0001:
            return "42"
        elif random.random() < 0.0001:
            raise RuntimeError("Black hole formed in code.")